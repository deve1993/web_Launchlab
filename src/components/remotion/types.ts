export interface AnimationLabels {
  top_bar: string;
  step1_label: string;
  step2_label: string;
  step3_label: string;
  step4_label: string;
  idea_text: string;
  postit_target: string;
  postit_problem: string;
  postit_solution: string;
  postit_market: string;
  layers_title: string;
  frame_label: string;
  font_label: string;
  status_preparing: string;
  status_building: string;
  status_deploying: string;
  status_deployed: string;
  live_badge: string;
  url_text: string;
}
